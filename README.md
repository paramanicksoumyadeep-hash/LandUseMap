I acknowledge that I have taken the help of AI tool Chatgpt to implement some of the questions like Assigning image label using the dominant (mode) land-cover class,filter satellite image, implementing the ResNet18 Model, but I have conceptual understanding of how the codes work and the uses of the libraries ,methods and functions used in the codes.
I have used RestNet18 for training the model. I gave a accuracy of 92.6% and a F1 score of 91.6%after training over the kaggle dataset provided.
The libraries used to create the model and solve the problems given are:-
geopandas, matplotlib, shapely, numpy, pandas, os, rasterio, torch, scikit-learn, torchvision, scipy, seaborn, pillow
